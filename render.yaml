services:
  - type: web
    name: piitrade
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: FLASK_DEBUG
        value: "0"
      - key: ALLOWED_ORIGINS
        value: "*"
